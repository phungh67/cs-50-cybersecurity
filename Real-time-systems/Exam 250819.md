# Problem 1
Consider this set of tasks. Every task contains 3 values: the execution time (worst case) $C_i$, the deadline (relative) $D_i$ and the period $T_i$


|          | $C_i$ | $D_i$ | $T_i$ |
| -------- | ----- | ----- | ----- |
| $\tau_1$ | 4     | 10    | 10    |
| $\tau_2$ | 3     | 25    | 25    |
| $\tau_3$ | 18    | 75    | 75    |
a. Perform the Liu & Layland's utilization-based analyse test for this given tasks set?

The formula of Liu & Layland's is
$$
U = \sum_{i=1}^{n} \frac{C_i}{T_i} \le n(2^{1/n} - 1)
$$
Plug the number, we have:
$U=0,76$ and the upper bound equals to $0,7797$ which proves that this set is sufficient with the Liu and Layland's utilization-based test.

b. Giving another task set, consider that these tasks are scheduled with rate-monotonic priority assignment. All tasks arrive at $t=0$.

|          | $C_i$ | $D_i$ | $T_i$ |
| -------- | ----- | ----- | ----- |
| $\tau_1$ | 6     | 12    | 16    |
| $\tau_2$ | 4     | 13    | 14    |
| $\tau_3$ | 20    | 70    | 75    |

Rate-monotonic priority means the task with least period value will be prioritized most, so the correct sequence is $\tau_2 < \tau_1 < \tau_3$

So with the Response Time Analysis `RTA`

**With $\tau_2$:**
$R_2 = C_2 < D_2 = 13$ => passed

**With $\tau_1$:**
$R_1^0 = C_1 = 6$
$R_1^1 = C_1 + \lceil \frac{R_1^0}{T_1} \rceil * C_2 = 6 + \lceil \frac{6}{16}  \rceil * 4 = 10$
$R_1^2 = C_1 + \lceil \frac{R_1^1}{T_1} \rceil * C_2 = 6 + \lceil \frac{10}{16}  \rceil * 4 = 10$
Convergence happens at the iteration 2 and 1, and the response time is $10 \le D_1 = 12$ so the task $\tau_1$ is valid

**With $\tau_3$:**
$R_3^0 = C_3 = 10$
$R_3^1 = C_3 + \lceil \frac{R_3^0}{T_3} \rceil * C_2 + \lceil \frac{R_3^0}{T_3} \rceil * C_1 = 20 + \lceil \frac{10}{75} \rceil * 4 + \lceil \frac{10}{75} \rceil * 6 = 30$
$R_3^2 = C_3 + \lceil \frac{R_3^1}{T_3} \rceil * C_2 + \lceil \frac{R_3^1}{T_3} \rceil * C_1 = 20 + \lceil \frac{30}{75} \rceil * 4 + \lceil \frac{30}{75} \rceil * 6 = 30$
Convergence happens at the iteration 2 and 1, and the response time is $30 \le D_3 = 75$ so the task $\tau_3$ is valid

c. For a given task set below, with the scheduler into a single processor with earliest-deadline-first `EDF`.


|          | $C_i$ | $D_i$ | $T_i$ |
| -------- | ----- | ----- | ----- |
| $\tau_1$ | 6     | 10    | 15    |
| $\tau_2$ | 5     | 8     | 30    |
| $\tau_3$ | 18    | 38    | 75    |

So that the $L_{max}=77$ for the given task set, prove it by both find the least common multiplier and the hyper-bound by using Baruah, Rosier and Howell formula.


To find the upper value, we apply the formula
$$
L_{BRH} = \max (D_1, D_2, D_3, D_{BRH})
$$
With
$$
D_{BRH} = \frac{\sum(T_i - D_i)*U_i}{1-U}
$$
In which U is the utilization, $U_i = C_i / T_i$
So we have the
$$
D_{BRH} = \frac{\sum(T_i - D_i)*U_i}{1-U} = \frac{(15-10)*\frac{6}{15} +(30-8)*\frac{5}{30} + (75 - 38) * \frac{18}{75}}{1 - \frac{6}{15} - \frac{5}{30} - \frac{18}{75}} = 75.24
$$
With the $LCM=150$ so that we have the $L_{BRH}=77$

Control point of given set (calculated based on the deadline)
- For task 1 $\tau_1$: 10, 25, 40, 55, 70
- For task 2 $\tau_2$: 8, 38, 68
- For task 3 $\tau_3$: 38
Merge all point, we have {8,10,25,38,40,55,68,70}

The formula to calculate the processor demand is
$$
C_p(0, L) = \sum^n_{i=1}(\lfloor\frac{L - D_i}{T_i}\rfloor + 1)C_i \le L
$$

| L   | $(\lfloor \frac{L - D_1}{T_1} \rfloor + 1)*C_1$ | $(\lfloor \frac{L - D_2}{T_2} \rfloor + 1)*C_2$ | $(\lfloor \frac{L - D_3}{T_3} \rfloor + 1)*C_3$ | $C_P(0,L)$ |      |
| --- | ----------------------------------------------- | ----------------------------------------------- | ----------------------------------------------- | ---------- | ---- |
| 8   | 0                                               | 5                                               | 0                                               | 5          | PASS |
| 10  | 6                                               | 5                                               | 0                                               | 11         | FAIL |
| 25  | 12                                              | 5                                               | 0                                               | 17         | PASS |
| 38  | 12                                              | 10                                              | 18                                              | 40         | FAIL |
| 40  | 18                                              | 10                                              | 18                                              | 46         | FAIL |
| 55  | 24                                              | 10                                              | 18                                              | 52         | PASS |
| 68  | 24                                              | 15                                              | 18                                              | 57         | PASS |
| 70  | 30                                              | 15                                              | 18                                              | 63         | PASS |

# Problem 2 (Diagram, so skip)

# Problem 3

Giving the below code block
```C
int FuncX(int x, int y){
	int t;
	if (y == 0){
		t = 1;
	} else {
		t = x *FuncX(x, y - 1);
		if (x % 2 == 0){
			t = t * 2;
		}
	}
	return t;
}
```

a. Derive the `WCET` formula for this case? Should be in the form of $k_1.x + k_2.y + k_3$ 
b. Why does the worst-case execution-time analysis of a piece of code should be as tight as possible.

a. Baseline case (in which $y=0$)
The $WCET_{baseline}$ will be:
- Declare costs 1
- Comparison costs 2
- Assign costs 1
- Return costs 2
So the $WCET_{baseline}=6$

In the branch which y is not zero:
- Declare costs 1
- Comparison costs 2
- Call the function cost: $WCET + 11$
	- Assign costs 1
	- `WCET(y-1)` and 2
	- Subtract costs 3
	- Multiply costs 5
- Modulo costs 4
- Logic costs 2
- Assign costs 1
- Multiply costs 5
- Return costs 2
In total $WCET_{branch}=28 + WCET(y-1)$

Summary:
$$
WCET = WCET_{baseline} + WCET_{branch} = 6 + y.28 = 0.x + 28.y + 6
$$
b.
A tight `WCET` bound reduces pessimism in schedulability analysis and prevents hardware over-provisioning. If a `WCET` estimate is too loose (massively overestimated), the total system utilization ($U$) or processor demand ($h(L)$) will artificially inflate. This will cause scheduling tests to falsely report the system as unschedulable, forcing engineers to buy faster, more expensive processors or remove features unnecessarily to fix a "problem" that doesn't actually exist in reality.

Long-short: tight reduces the pessimism in the analysis for prevents the hardware over-provisioning[^1]

>[! Note from lecture]
>Tight to avoid unnecessary pessimism in the schedulability analysis, which could cause feasibility tests to be too inaccurate to be useful.

[^1]: Lecture slides 7

c. Describe the meaning of priority inversion in the context of single-processor scheduling. Explain the concept of priority inheritance protocol (PIP) as used in access-control protocols for critical regions. Would it be correct to say that the PIP is a deadlock-free protocol? Motivate your answer?

- Priority inversion occurs when a high-priority task is blocked from executing because it is waiting for a shared resource (like a mutex) that is currently held by a lower-priority task.
- The Priority Inheritance Protocol (PIP) solves unbounded priority inversion by temporarily elevating the priority of the lower-priority task holding the resource to match the priority of the highest-priority task waiting for it. Once the resource is released, the task drops back to its original priority.
- **Is PIP deadlock-free?** **No.** PIP is completely vulnerable to deadlocks if tasks lock multiple mutexes in different orders, creating a circular wait (e.g., `T1`​ holds `S1`​ and waits for `S2`​, while `T2`​ holds `S2`​ and waits for `S1`​).

d. Consider a task set with independent periodic tasks, for which it applies that the deadline of each task is equal to its period. Assume that the task set is known to be not schedulable on a single-processor system using static task priorities. Would a necessary feasibility test report the answer ’True’ or ’False’ when applied to that task set? Motivate your answer! 

- **Answer:** It could report **either 'True' or 'False'**.
- **Motivation:** By definition, a necessary test checks the bare minimum conditions required for schedulability (e.g., total utilization $U≤1$). Just because a task set is known to be _not_ schedulable does not mean it failed the necessary test; it might have passed the necessary test (reporting 'True') but failed due to other factors like worst-case blocking times or interference. It could also report 'False' if it is severely overloaded. Therefore, you cannot definitively say it will report one or the other without looking at the exact task parameters.

e. The `TinyTimber` run-time scheduler supports the concept of concurrent programming. Describe how the application programmer instructs the `TinyTimber` kernel to create a concurrent execution of a given piece of program code.

- **Answer:** The application programmer creates concurrent execution by using the **`ASYNC()`** macro.  
- **Context:** `ASYNC(object, method, argument)` instructs the `TinyTimber` kernel to place the method invocation into the ready queue to be executed concurrently as an independent thread/task, rather than executing it sequentially like a standard C function call.

f. The `TinyTimber` run-time scheduler uses a system clock to support timing-aware programming. De-scribe how the application programmer instructs the `TinyTimber` kernel to read the current value of the system clock. Also state the resolution (that is, the length of a time unit) of the `TinyTimber` system clock in the laboratory system used in the course. 

- **How to read the clock:** The programmer instructs the kernel to read the current system clock value by calling the **`CURRENT_OFFSET()`** macro.
- **Resolution:** In standard real-time laboratory environments using `TinyTimber`, the time resolution of the system clock is **1 microsecond (1μs)**. (This is why the API provides macros like `USEC()` and `MSEC()` to handle these precise timer ticks).
